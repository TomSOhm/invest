# Optional ML dependencies for sentiment scoring (M9, opt-in).
#
# Install via:
#     pip install -r backend/requirements-ml.txt
#
# These are NOT pulled by the main backend install.  Without them every
# sentiment_score / sentiment_signals_df call returns NaN and the rest of
# the engine continues to work unchanged.
torch>=2.12.0,<3.0
transformers>=5.8.1,<6.0
