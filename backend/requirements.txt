# LEGACY — kept for Docker/CI compatibility.
# Source of truth is now pyproject.toml [dependency-groups] dev + uv.lock.
# Local dev should use `make install` (which runs `uv sync --group dev`).
fastapi>=0.136.1
uvicorn[standard]>=0.34.0
pydantic>=2.13.4
pyyaml>=6.0
python-dotenv>=1.2.2
yfinance>=0.2.40
pandas>=2.1.0
numpy>=1.24.0
loguru>=0.7
httpx>=0.27.0
protobuf>=7.35.0 # not directly required, pinned by Snyk to avoid a vulnerability
