# ═══════════════════════════════════════════
# Invest Solo — Python Dependencies
# ═══════════════════════════════════════════
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════

# ── Core Data ──
yfinance>=0.2.30
pandas>=2.0
numpy>=1.24
scipy>=1.11
requests>=2.31

# ── Financial Analysis ──
financetoolkit>=1.9
# PyValuation>=0.1          # Optional: DCF & relative valuation
# openbb>=4.0               # Optional: comprehensive terminal SDK

# ── Technical Analysis ──
# ta-lib                    # Requires C library: brew install ta-lib (Mac) / apt install libta-lib-dev (Linux)
ta>=0.10                    # Pure Python alternative to ta-lib
pandas-ta>=0.3              # Additional technical indicators

# ── Visualization ──
plotly>=5.18
matplotlib>=3.8
seaborn>=0.13
streamlit>=1.30             # Interactive dashboard

# ── Machine Learning (Phase 5) ──
scikit-learn>=1.3
statsmodels>=0.14
# xgboost>=2.0              # Optional: gradient boosting
# lightgbm>=4.0             # Optional: faster gradient boosting

# ── Report Generation ──
python-docx>=1.0
openpyxl>=3.1
fpdf2>=2.7
jinja2>=3.1

# ── Web Scraping (for PEA eligibility lists) ──
beautifulsoup4>=4.12
lxml>=4.9

# ── Configuration & Utilities ──
pyyaml>=6.0
python-dotenv>=1.0
loguru>=0.7
click>=8.1                  # CLI interface
rich>=13.0                  # Beautiful terminal output
schedule>=1.2               # Task scheduling
tqdm>=4.65                  # Progress bars

# ── Data Storage ──
sqlalchemy>=2.0             # Optional: database backend
# duckdb>=0.9               # Optional: fast analytics on local data

# ── Testing ──
pytest>=7.4
pytest-cov>=4.1
